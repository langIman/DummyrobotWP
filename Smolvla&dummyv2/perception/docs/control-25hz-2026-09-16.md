# 25 Hz 控制更新

0.7.2 将浏览器输入、Servo 目标输出、感知下发循环和使能/录制时的六轴反馈均设为
40 ms；保留静止未使能时按需读取。速度仍为 0.10 m/s / 0.24 rad/s，流式速度参数为 8。

桥接对 `#GETJPOS` 的完整六轴换行回复提前返回，原 100 ms 只作异常等待上限；
运动命令继续等待真实确认。循环把通信耗时计入周期，没有超时后的补发队列。
网页同时显示设定及最近 5 秒实测读取/下发频率，统计容量覆盖 25 Hz。

验证：协议分包、半包和日志包装回归通过；控制调度覆盖 15 ms 通信耗时仍为 40 ms 周期；
整体测试及桌面/移动端网页检查通过。实际 MoveIt 在隔离 ROS 域运行 25 Hz 假驱动闭环，
输出 150 次目标，最大目标差约 1.72°，松键保持、停滞暂停和碰撞停止通过；105 mm
偏移的 W/D/鼠标方向及 TCP 补偿验证通过。未向实机发送使能或运动命令。

部署已确认 ROS `publish_period=0.04`、感知读取/下发设定均为 25 Hz。
实机只读测速未完成：2026-09-16 18:50 左右桥接报告 `open_error: device_not_found`，
Windows 仅枚举到蓝牙 COM15/COM16，未识别机械臂串口；因此不宣称实机已达到 25 Hz。

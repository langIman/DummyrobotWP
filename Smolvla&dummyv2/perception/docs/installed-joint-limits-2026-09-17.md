# 实机主控关节限位核对

本次离线读取 `runtime/speed-audit-20260917-104822/flash.bin` 和 `sram.bin`。
Flash SHA-256 为 `F52D9209FE2EECA72B6EB0235E6C5446C2A326B2A0F1A6F4ECAF6C590076C5AF`，
与此前核验的实机备份一致；未连接调试器、暂停主控或发送测试运动。

对象 `0x200035A8 + 0xE4` 开始是六轴对象指针。
实机 MoveJ 反汇编分别比较轴对象 `+0x0C` 上限和 `+0x10` 下限，
越界返回 false；J3 的比较位于 `0x08011A98` 至 `0x08011AB8`。
这不是根据外观或其他版本源码推测出来的范围。

| 轴 | 对象地址 | 下限 ° | 上限 ° |
|---|---|---:|---:|
| J1 | 0x20006F88 | -170 | 170 |
| J2 | 0x20006FD0 | -75 | 90 |
| J3 | 0x20007018 | 35 | 180 |
| J4 | 0x20007060 | -180 | 180 |
| J5 | 0x200070A8 | -100 | 100 |
| J6 | 0x200070F0 | -180 | 180 |

13:56:02.205 主机发送 J3=35.403°，13:56:02.241 发送 J3=33.143°；
随后反馈反复保持 J3=35.40°，主机目标却继续减小至 -5°。
串口层的 ok 不能替代主控 MoveJ 对目标合法性的确认。

0.8.14 使用 `motion/joint_contract.py` 统一轨迹生成、发送检查和运行时 URDF 限位。
模型转换为 `q_model = radians(q_hardware) * direction - offset`，其中
J3 offset=π/2，J5/J6 direction=-1。不会修改原始模型、原仓库或固件。
这份范围来自上述版本快照；今后若更换主控固件，需要重新核对。
